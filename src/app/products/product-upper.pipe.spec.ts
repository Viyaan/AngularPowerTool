import { ProductUpperPipe } from './product-upper.pipe';

describe('ProductUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
